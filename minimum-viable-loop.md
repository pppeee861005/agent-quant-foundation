# 最小可行閉環（Minimum Viable Loop）

> 這份文件提供「**最小閉環**」的設計圖。
> 故意不提供完整程式碼——讓 agent 在了解結構後自行實作。

## 為什麼從最小閉環開始

許多人開始量化專案時的失敗模式是「**先把整個系統設計完美才開始寫**」。然後在第三週還沒寫完，動力消失。

最小閉環的目的相反——**在最短時間內讓三層之間有資料流動**，哪怕功能很弱、策略很笨。閉環一旦跑通，後續迭代會自然發生。

## 最小閉環的目標

讓三層之間發生一次完整的資料流：

```
人類指揮官
   ↓ (手動寫死規則)
執行層
   ↓ (訂閱報價)
感知層 (富途 OpenAPI)
   ↓ (推送即時報價)
執行層
   ↓ (規則觸發)
感知層 (下模擬盤單)
   ↓ (回報成交)
執行層
   ↓ (推 Telegram)
人類指揮官（手機收到通知）
```

注意：**最小閉環刻意不接大腦層**。先讓感知與執行打通，再加大腦——這是為了避免一次處理太多複雜度。

## 五個檢查點

最小閉環跑通的判斷標準是這五件事都發生了：

1. ✅ 感知層連線成功（執行層能拉到一檔股票的當下報價）
2. ✅ 訂閱推送運作（執行層能持續收到報價更新）
3. ✅ 規則觸發（執行層判斷「**跌幅 > X%**」會觸發行為）
4. ✅ 模擬盤下單成功（感知層回傳成交訊息）
5. ✅ 通知到達（指揮官手機收到 Telegram 推播）

達成這五點，第一個閉環就完成。

## 三項原則的最小實作

最小閉環雖然簡單，但三項原則仍要實作：

### 篩選權的最小實作

- 標的清單由指揮官**手動**寫在設定檔（不從外部來源拉）
- 設定檔修改需重啟系統（避免 agent 動態改寫）

### 意義權的最小實作

- `MAX_POSITION_PCT`、`MAX_DAILY_LOSS` 是程式碼裡的常數
- `TRADING_ENABLED` 是環境變數
- 下單前檢查所有風控旗標

### 斷線權的最小實作

- 系統開頭有 `should_run_now()` 函式檢查日期、時段、kill switch
- Telegram 通知裡內含 kill switch 觸發指令
- 連續 3 次 API 呼叫失敗自動進入安全模式

## 接上大腦層的時機

最小閉環跑通後，再考慮接大腦層。

接大腦層的好時機：

- 你發現「**跌 X% 就買**」這個簡單規則太蠢，需要更多脈絡
- 你開始想要每週讀一些研究報告影響選股池
- 你想要記錄「**為什麼這次決定不下單**」的可讀理由

接大腦層的壞時機：

- 還沒跑通最小閉環就想接（複雜度爆炸）
- 為了「**看起來更 AI**」而接（沒有真實需求）
- 想用大腦層繞過硬編碼風控（違反意義權，禁止）

## 工作節奏建議

| 週次 | 目標 | 產出 |
|---|---|---|
| Week 1 | 感知層連通 | OpenD 啟動、SDK 連線、拉到第一檔報價 |
| Week 2 | 執行層 + 感知層閉環 | 規則觸發 → 模擬下單 → Telegram 通知 |
| Week 3 | 三項原則加固 | 風控常數、kill switch、安全模式 |
| Week 4 | （可選）接大腦層 | 大腦層判斷 → 影響選股池 |

每一週結束時建議寫一篇短戰報——這是《新人類聯盟養成日記》系列的素材。

## 給 agent 的指示

當使用者載入此 Skill 並表達想做模擬盤學習時：

1. 引用此文件的「**五個檢查點**」作為里程碑
2. 不要替使用者寫完整程式碼——引導他自己寫，agent 只在卡關時提供具體建議
3. 每完成一個檢查點，提醒使用者記錄一份戰報
4. 完成五個檢查點後，回到 SKILL.md 的設計哲學——詢問使用者要不要把樣板搬到下一個垂直

---

*最小閉環的價值不在於它的功能，在於它讓三層真實互動過一次。一旦你看過這個閉環跑起來，你對整個架構的理解會比讀十篇文章更深。*
