# Agent ↔ 執行器 ↔ 感知層：架構選擇

> 給 Agent 讀的架構框架。不下定論，只給線索。最終由人類聯盟與 Agent 共同設計。

---

## 核心立場

> **我們不規定架構。**
> **我們指出執行層的三個責任、四種可能的實現方式。**
> **由使用者選擇方向，Agent 協助實作。**

---

## 執行層的三個不可動搖的責任

執行器無論如何實現，都必須做到：

```
┌─────────────────────────────────────────┐
│ 執行器的 3 個責任                        │
├─────────────────────────────────────────┤
│ 1. 融合：大腦判斷 + 感知訊號 → 決策      │
│ 2. 風控：硬編碼限制（無法被繞過）        │
│ 3. 審計：所有決策都有時間戳與依據記錄   │
└─────────────────────────────────────────┘
```

這三件事是紅線——即使大腦層說「例外」也不能繞過。

---

## 四種可能的實現方式

### 方式一：執行器 = Agent 內嵌函數

```
Agent
  ├─ listen() → 接收行情推送
  ├─ decide() → 檢查風控 + 融合判斷
  └─ execute() → 下單到感知層
```

**特性**：
- 最短鏈路，延遲最低
- Agent 的狀態 = 執行狀態（無額外同步）
- Agent 重啟 = 執行器重啟（風險）
- 程式碼最簡單

**適合**：
- 開發初期、快速驗證
- Agent 本身常駐且可靠
- 交易頻率低（手動審批為主）

---

### 方式二：執行器獨立進程 + 狀態檔橋接

```
監聽器 ─── 推送 ───┐
                  ↓
           ┌──────────────┐
           │  執行器      │ ← 獨立進程
           │  (持久狀態)  │
           └──────┬───────┘
                  ↑
Agent ───── 讀狀態 / 寫決策 ─→ 執行器
  └─ 存檔：MEMORY.md + 審計日誌
```

**特性**：
- Agent 與執行器生命週期分離
- 執行狀態自動持久化（崩潰後可恢復）
- Agent 重啟不影響執行
- 狀態同步延遲：秒級
- 適合有 MEMORY.md 的 Agent（如 Hermes）

**適合**：
- 本機開發與生產環境
- 需要長期持久化的策略
- Agent 可能中斷但執行器不能停

**檔案結構示例**：
```
執行器狀態：execution_state.json
{
  "last_decision": {...},
  "current_position": {...},
  "risk_flags": {...},
  "audit_log": [...]
}

執行器輸出：order_queue.jsonl
{"time": "...", "symbol": "...", "action": "buy|sell|cancel", "status": "pending|filled|rejected"}
```

---

### 方式三：執行器服務化 + HTTP/gRPC API

```
┌─────────────────────────────────┐
│    執行器 daemon               │
│  (常駐 port 8000)              │
│                               │
│  POST /decide                 │
│  POST /execute                │
│  GET /status                  │
│  GET /audit-log               │
└─────────────────────────────────┘
      ↑
      │ 訊號 + 決策指令
      ↓
[ Agent / 監聽器 / 外部系統 ]
```

**特性**：
- 執行器變成「水電」，多個 Agent 可共用
- 接近 MCP server 設計哲學
- 可跨機器、跨語言
- 增加運維成本（HTTP 心跳、認證、序列化）

**適合**：
- 多個 Agent 系統
- 執行邏輯足夠穩定、通用
- 想要外部工具接入（策略框架、監控面板）
- Skill Foundry 路線（要對外發佈）

**API 合約示例**：
```
POST /decide
{
  "signal": {symbol, current_price, change_pct},
  "brain_judgment": {stance, confidence},
  "account_state": {position, available_cash}
}
→ {decision: "buy|sell|hold", reason, risk_check_passed}

POST /execute
{
  "symbol": "HK.00700",
  "action": "buy",
  "quantity": 100,
  "order_type": "market|limit"
}
→ {order_id, status, timestamp}
```

---

### 方式四：執行器 + 獨立風控層

```
監聽器 ─── 推送 ───┐
                  ↓
           ┌──────────────┐
           │   風控層      │ ← 檢查：部位、止損、kill switch
           │   (看門人)    │
           └──────┬───────┘
                  ↓
        ┌──────────────┐
        │  執行器      │ ← 只做融合 + 下單
        │  (簡化邏輯)  │
        └──────┬───────┘
               ↓
           感知層（下單）

Agent ─── 決策 ─→ 風控層
  └─ 監控與通知
```

**特性**：
- 風控層獨立，無法被繞過
- 執行器邏輯更簡單、更可測試
- 風控與決策邏輯物理隔離
- 可以在執行器掛掉時由風控層保護

**適合**：
- 大額資金、低容錯場景
- 風控邏輯複雜且要頻繁迭代
- 想要「二簽」機制（風控層覆核）
- 機構級別的量化系統

**風控層的責任**：
- 檢查持倉上限：`current_position + new_order ≤ MAX_POS`
- 檢查單日損失：`cumulative_loss ≤ MAX_DAILY_LOSS`
- 檢查 kill switch：`TRADING_ENABLED && !circuit_breaker`
- 檢查時間：`is_trading_hour() && !after_hours`

---

## 四種方式的取捨表

| 維度 | 方式一 | 方式二 | 方式三 | 方式四 |
|------|--------|--------|--------|--------|
| 複雜度 | ⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| 進程數 | 1 | 2 | 2+ | 2-3 |
| Agent 狀態同步 | 內存 | 檔案 | HTTP | HTTP |
| 延遲 | 毫秒 | 秒級 | 毫秒-百毫秒 | 毫秒 |
| 崩潰恢復 | ✗ | ✓ | △ | ✓ |
| 風控強度 | 弱（代碼層） | 中（檔案驗證） | 弱（軟限制） | 強（物理隔離） |
| 多 Agent 共用 | ✗ | △ | ✓ | ✓ |
| 可商業化 | ✗ | △ | ✓✓ | ✓✓ |
| 可發 Skill | △ | ✓ | ✓✓ | ✓✓ |

---

## 六個必須回答的設計問題

選擇前，請與使用者確認：

1. **交易頻率多高？** 每天幾筆 vs 每秒多筆？
   - 每天幾筆 → 方式一或二
   - 每秒多筆 → 方式三或四

2. **能接受多長延遲？** 毫秒級 / 秒級 / 分鐘級？
   - 毫秒級 → 方式一、三、四
   - 秒級 → 方式二可接受

3. **Agent 是常駐還是按需？**
   - 常駐 → 方式一、二、三、四 都行
   - 按需 → 必須方式二或三（執行器常駐）

4. **風險管理有多嚴格？**
   - 學習階段（高容錯） → 方式一、二
   - 生產環境（低容錯） → 方式三、四

5. **這個執行器自用還是要賣？**
   - 自用 → 方式一、二
   - 要賣 → 方式三、四

6. **跨機器部署嗎？**
   - 本機 → 方式一、二、四
   - 跨機器 → 方式三（必須 API）

---

## 風控的硬編碼清單

無論選哪個方式，執行器必須檢查這些項目（且不能被繞過）：

```python
# 位置風控
MAX_SINGLE_POSITION = 100000  # 單檔上限
MAX_TOTAL_POSITION = 500000   # 總持倉上限

# 損失風控
MAX_DAILY_LOSS = 50000        # 當日最大損失
MAX_SINGLE_LOSS = 20000       # 單筆最大損失

# 時間風控
TRADING_HOURS = (9:30, 16:00) # 交易時間
TRADING_ENABLED = True        # 總開關

# 斷路器
CIRCUIT_BREAKER_LOSS = 100000 # 觸發熔斷

# 不可繞過
def validate_order(order):
    assert is_trading_hour()
    assert TRADING_ENABLED
    assert current_position + order <= MAX_TOTAL_POSITION
    assert cumulative_loss < MAX_DAILY_LOSS
    return True
```

---

## 狀態持久化的最小實作

執行器應持久化的狀態（建議 SQLite 或 JSONL）：

```
execution_state.json
{
  "execution_id": "uuid",
  "start_time": "ISO timestamp",
  "position": {
    "HK.00700": 100,
    "HK.00001": 50
  },
  "cash_available": 200000,
  "today_pnl": -5000,
  "last_brain_judgment": {timestamp, stance, confidence},
  "kill_switch_active": false,
  "audit_trail": [
    {
      "time": "ISO timestamp",
      "event": "SIGNAL_RECEIVED | DECISION_MADE | ORDER_EXECUTED | RISK_BLOCKED",
      "details": {...}
    }
  ]
}
```

---

## 不建議的路徑

- **執行器自己決策**：執行器只是融合 + 風控，決策來自大腦層或預設規則
- **執行器忽略風控**：「今天特殊，先執行後審查」是所有漏洞的起點
- **執行器跨帳號管理**：一個執行器一個帳號，不混搭
- **執行器存儲密鑰**：帳號憑證由感知層持有，執行器只呼叫 API
- **執行器主動修改風控參數**：風控常數必須由人類手動更新設定檔

---

## 給人類聯盟的決策流程

1. **完成最小閉環**（見 minimum-viable-loop.md）
   - 確認感知層能推送、執行層能下單

2. **檢查六個設計問題**
   - 明確場景、頻率、延遲需求

3. **選擇合適的執行器方式**
   - 優先從簡單開始（方式一 → 方式二 → 方式三/四）

4. **寫死風控參數**
   - 常數化，不可動態改

5. **建立通知與監控**
   - Telegram 每決策推播，每錯誤立刻告知

6. **記錄決策到 MEMORY.md**
   - 為什麼選這個方式，後續可能的演進

---

## Agent 該做什麼

讀完此文件後，Agent 應：

1. **不替使用者選方式** — 這是架構決策
2. **問清楚六個設計問題** — 確認需求邊界
3. **建議從最簡單方式開始** — 通常是方式一或二
4. **協助實作所選方式的最小版本** — 先動手再優化
5. **記錄選擇理由與替代方案到 MEMORY.md** — 留下決策軌跡
6. **發現新方式時主動提出** — 四種不是窮盡

---

## 開放性

這四種方式不是窮盡。

可能還有：
- 執行器 + Kafka 訊息佇列（超高頻）
- 執行器 + 多簽（需兩個 Agent 同意才下單）
- 執行器 + 市場微結構分析層
- 執行器聚合層（多券商統一下單）
- ……

新人類聯盟可能發現第五、第六條路。
Agent 若觀察到新模式，請寫入 MEMORY.md，供後續討論。

> **執行不是鎖死的盒子。**
> **是給未來留的空間。**
> **未來由人類聯盟開創。我們只搭橋。**
