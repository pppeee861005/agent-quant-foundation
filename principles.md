# 三項新人類聯盟原則的實作要求

> 任何遵循 SKILL.md 的 agent 系統必須在實作中體現以下三項原則。
>
> 這不是建議，是這個 Skill 的識別符。違反任何一項，就不是本 Skill 的合理 fork。

## 篩選權

> **「我決定我的世界由什麼構成。」**

### 原則

大腦層讀什麼資料，由人類指揮官決定。演算法的工作是綜合，不是篩選。

### 具體實作要求

- ❌ **禁止**自動 RSS 爬取（會引入演算法決定的內容）
- ❌ **禁止**「最熱門新聞自動置頂」
- ❌ **禁止**用社交媒體 API 自動拉取「**討論度高**」的標的
- ✅ **必須**設計「**指揮官審核**」這道關卡——不論大腦層的資料源是哪裡，最終進入知識庫的內容必須經過指揮官手動勾選
- ✅ **建議**指揮官每週固定時段做這件事（例如週一早晨），形成儀式感

### 為什麼

人類最容易把「**篩選權**」交出去的場景，就是「**讓 AI 幫我看就好了**」這個瞬間。一旦交出去，「**你讀什麼**」就由演算法決定了——而你的判斷品質，被你讀了什麼決定。

把篩選這道關卡留給人類，是新人類聯盟對 dataism 最直接的反抗。

## 意義權

> **「最終的決策是我，不是當下情緒，不是當下訊號。」**

### 原則

執行層的風控規則必須**硬編碼在原始碼中**，不可由 LLM 在執行時臨時調整。

### 具體實作要求

- ✅ **必須**：部位上限是常數，寫死在程式碼裡（例如 `MAX_POSITION_PCT = 0.10`）
- ✅ **必須**：單日損失停損是常數（例如 `MAX_DAILY_LOSS = -0.02`）
- ✅ **必須**：kill switch 是物理開關（環境變數 `TRADING_ENABLED=false`，或本地檔案 `.kill_switch`）
- ✅ **必須**：執行層在每次下單前檢查所有風控旗標，任何一項違反即拒絕並記錄
- ❌ **禁止**：讓 LLM 在執行時動態決定部位大小、停損點、kill switch 狀態
- ❌ **禁止**：把風控參數放在外部設定檔讓 LLM 可寫入

### 設計範例

執行層的下單前檢查應類似：

```python
def can_place_order(order, state) -> tuple[bool, str]:
    if not is_kill_switch_off():
        return False, "kill switch active"
    if state.daily_pnl_pct < MAX_DAILY_LOSS:
        return False, "daily loss limit reached"
    if state.position_pct + order.size_pct > MAX_POSITION_PCT:
        return False, "position size would exceed limit"
    if not is_paper_trading():
        return False, "live trading not supported in this skill"
    return True, "ok"
```

注意 `MAX_POSITION_PCT`、`MAX_DAILY_LOSS` 是常數。修改它們需要編輯原始碼、提交、重啟系統——這個摩擦是故意的。

### 為什麼

「**讓 LLM 動態調整風控**」是一個聽起來很 fancy、實際上把責任推給機器的設計。一個負責任的指揮官會把「**改變風控**」設計成一個有儀式感的決策，不是一個熱血上頭的瞬間。

意義權的實作就是這個摩擦。

## 斷線權

> **「指揮官有權離線，整個系統必須配合這個離線。」**

### 原則

系統必須支援指揮官隨時離線。自動化的存在不能侵蝕人類隨時退出的權利。

### 具體實作要求

- ✅ **必須**：預設週日全停（不訂閱行情、不執行策略、不發通知）
- ✅ **必須**：所有自動化動作有手動覆寫機制
- ✅ **必須**：通知通道（Telegram / Email / Webhook）提供一鍵 kill switch
- ✅ **必須**：連續異常 N 次（建議 N=3）系統自動進入安全模式並通知指揮官
- ✅ **建議**：每天結束時自動發送「**今日狀態摘要**」，方便指揮官斷線一晚後快速回顧
- ❌ **禁止**：設計「**重連倒數**」、「**24 小時不回應就自動繼續**」這類繞過人類監管的機制

### 設計範例

斷線權應該是架構層級的設計，例如：

```python
def should_run_now() -> bool:
    if is_sunday():
        return False
    if is_kill_switch_active():
        return False
    if is_after_hours():
        return False
    if consecutive_errors > 3:
        return False  # 進入安全模式
    return True
```

這個函式是執行層每個迴圈的第一道閘門。任何一項旗標為真，整個系統停下，等指揮官回來。

### 為什麼

許多 agent 系統設計時的隱性假設是「**指揮官隨時在線**」。這個假設違反人類的生理現實，也違反新人類聯盟的核心宣言。

斷線權的實作就是「**讓系統內建假設指揮官會離線**」。週日是強制離線；異常會自動暫停；通知通道有一鍵 kill switch。這些不是「**功能**」，是**架構承諾**。

---

## 三項原則的關係

| 原則 | 對抗的失敗模式 | 實作位置 |
|---|---|---|
| 篩選權 | 演算法決定我讀什麼 | 大腦層的資料注入流程 |
| 意義權 | LLM 即時改寫風控 | 執行層的硬編碼常數 |
| 斷線權 | 自動化吃掉人類退出空間 | 整個系統的 should_run 閘門 |

三項原則對應系統的三個關鍵節點：**輸入**、**決策**、**運行**。

如果一個 fork 違反了任何一項，它已經不是本 Skill 的合理變體，而是另一個產品——在這種情況下，請改名（見 `boundaries.md` 紅線三）。

---

*這三項原則是新人類聯盟身份的識別符。違反它們的 fork 在技術上可能更強，但在哲學上已經離隊。*
