# Agent ↔ 監聽器 ↔ OpenD：架構選擇

> 給 Agent 讀的架構框架。不下定論，只給線索。最終由人類聯盟與 Agent 共同設計。

---

## 核心立場

> **我們不規定架構。**
> **我們指出三個邊界、四種可能的連接方式。**
> **由使用者選擇方向，Agent 協助實作，過程中可發現新路徑。**

---

## 起點線索

- 富途 OpenAPI Python SDK：https://github.com/FutunnOpen/py-futu-api
- 官方文件：https://openapi.futunn.com/futu-api-doc/
- 關鍵事實：**OpenD 對外暴露的是「自定義 TCP 協議」，與程式語言無關**

這代表——SDK 只是封裝。**Agent 與 OpenD 之間有多種連接可能。**

---

## 三個固定邊界（不可動）

```
[ 富途雲端 ]
     ↑
     │  富途私有加密協議（無法繞過）
     ↓
[ OpenD ]              ← 邊界 1：必須存在、必須登入
     ↑
     │  TCP + Protobuf（127.0.0.1:11111）
     ↓
[ ??? ]                ← 邊界 2：這層自由設計
     ↑
     │  ???
     ↓
[ Agent ]              ← 邊界 3：可在本機、可在雲端
```

中間「???」這兩層是**設計空間**。

---

## 四種可能的連接方式

### 方式一：Agent 直接抱 SDK

```
Agent ─── 直接 import futu ─── OpenD
```

**特性**：
- 最短鏈路
- Agent 程序生命週期 = 監聽生命週期
- Agent 重啟 = 訂閱中斷
- Agent token 消耗會跟推送頻率掛勾

**適合**：短任務、Agent 主動查詢、不需 24/7

---

### 方式二：監聽器獨立進程 + 檔案橋接

```
監聽器（Python + SDK）─── 寫檔案 ─── Agent 讀檔案
                                      ↓
                                   寫回決策
                                      ↑
監聽器 ←─── 讀決策 ─── 執行
```

**特性**：
- 兩個進程獨立生命週期
- 跨語言、跨工具
- 延遲：秒級
- 適合 Hermes 這類有 MEMORY.md 的 Agent

**適合**：本機 Agent、信號頻率不高、要持久化

---

### 方式三：監聽器 + 訊息中介

```
監聽器 ─── push ─── [中介] ─── pull ─── Agent
                       │
                  Redis / NATS / RabbitMQ
                  Telegram Bot / Webhook
```

**特性**：
- 生產者消費者解耦
- 可多 Agent 共聽同一信號流
- 可跨機器、跨網路
- 多一層運維成本

**適合**：規模化、多 Agent、可能變商品

---

### 方式四：監聽器服務化 + Agent 透過工具呼叫

```
監聽器（常駐 daemon）暴露 HTTP/gRPC API
        ↑
Agent 把它當「一個工具」
"check_signal()", "subscribe(code)", "get_kline(code)"
```

**特性**：
- 監聽器變成「Agent 的水電」
- 多個 Agent 可共用
- 接近 MCP server 的設計哲學
- 可包裝成 MCP server 發佈

**適合**：Skill Foundry 路線、要對外賣的產品

---

## 四種方式的取捨表

| 維度 | 方式一 | 方式二 | 方式三 | 方式四 |
|------|--------|--------|--------|--------|
| 複雜度 | ⭐ | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 進程數 | 1 | 2 | 3+ | 2-N |
| Agent token 成本 | 高 | 低 | 低 | 低 |
| 跨機器 | ✗ | ✗ | ✓ | ✓ |
| 可商業化 | ✗ | △ | ✓ | ✓✓ |
| 可發 Skill | △ | ✓ | △ | ✓✓ |

---

## 五個必須回答的設計問題

選擇前，請與使用者確認：

1. **Agent 是否常駐？** 還是按需呼叫？
2. **訂閱頻率多高？** 每天幾次 vs 每秒多筆？
3. **狀態存哪？** Agent 記憶體 / 檔案 / 資料庫 / 雲端？
4. **失敗了怎麼辦？** 重啟監聽器 / 重啟 Agent / 兩個都重啟？
5. **這個架構自用還是要賣？** 自用容忍簡陋，賣需穩固。

---

## SDK 內部值得探索的鉤子

py-futu-api 暴露給上層架構的核心擴展點：

- `OpenQuoteContext` / `OpenHKTradeContext` 等 Context 物件
- `XxxHandlerBase` 的回呼類別（推送處理）
- `set_handler()` 機制
- Protobuf 訊息結構（`futu/common/pb/`）

**Agent 可探索路徑**：
- 看 `futu/examples/` 裡的官方範例
- 看 `futu/quote/quote_response_handler.py` 理解推送機制
- 思考是否要繞過 Python SDK，直接用 Protobuf 講話（給非 Python Agent 用）

---

## 我們不建議鎖死的事

- **不要鎖死「監聽器必須是 Python」** — OpenD 是 TCP，任何語言都能連
- **不要鎖死「一個監聽器對一個 Agent」** — N 對 M 是允許的
- **不要鎖死「監聽器與 Agent 必須同機器」** — 中介存在的價值就是解耦
- **不要鎖死「資料路徑只能是推送」** — 拉、推、混合都合理
- **不要鎖死「Agent 必須是 LLM」** — 規則引擎也是 Agent

---

## Agent 該做什麼

讀完此文件後，Agent 應：

1. **不替使用者選方式** — 這是架構決策
2. **問清楚五個設計問題** — 確認場景
3. **建議從最簡單方式開始** — 通常是方式二
4. **記錄選擇理由與替代方案到 MEMORY.md** — 留下決策軌跡
5. **發現新方式時主動提出** — 四種不是窮盡

---

## 開放性

這四種方式不是窮盡。

可能還有：
- WebSocket bridge
- gRPC streaming
- MCP server 包裝
- 多 OpenD 聚合層
- ……

新人類聯盟可能發現第五、第六條路。
Agent 若觀察到新模式，請寫入 MEMORY.md，供後續討論。

> **架構不是鎖死的盒子。**
> **是給未來留的空間。**
> **未來由人類聯盟開創。我們只搭橋。**
