# Futu API 學習筆記

## 資源

- Futu SDK: https://github.com/FutunnOpen/py-futu-api
- NotebookLM: https://github.com/win4r/notebooklm-py

## 架構

Python 腳本 --TCP(127.0.0.1:11111)--> FutuOpenD --加密協議--> 富途雲端 --> 交易所

重點:
- Python 使用 futu-api SDK 連線到本機 FutuOpenD
- FutuOpenD 是富途官方程式，需安裝、登入帳號
- FutuOpenD 與富途雲端通訊自動處理（SDK 幫你處理心跳）

## 程式範例

### 查詢行情（同步）

```python
import futu as ft

quote_ctx = ft.OpenQuoteContext(host="127.0.0.1", port=11111)
ret, data = quote_ctx.get_market_snapshot(['HK.00700'])

if ret == ft.RET_OK:
    print(data)  # DataFrame 格式
else:
    print(f"查詢失敗: {data}")

quote_ctx.close()
```

### 推送行情（非同步）

```python
import futu as ft
import time

class MyQuoteHandler(ft.StockQuoteHandlerBase):
    def on_recv_rsp(self, rsp_pb):
        ret_code, data = super().on_recv_rsp(rsp_pb)
        if ret_code == ft.RET_OK:
            print(data)  # 新報價時呼叫
        return ret_code, data

quote_ctx = ft.OpenQuoteContext(host="127.0.0.1", port=11111)
quote_ctx.set_handler(MyQuoteHandler())
quote_ctx.subscribe(['HK.00700'], [ft.SubType.QUOTE])

time.sleep(60)  # 接收推送
quote_ctx.close()
```
