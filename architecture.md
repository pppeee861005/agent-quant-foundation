# 架構：三層責任邊界

> 此文件供載入 SKILL.md 的 agent 在規劃實作時參考。

## 為什麼是三層

不是因為三層好看，是因為三件事的時間尺度與資料形態根本不同：

| 工作 | 時間尺度 | 資料形態 | 適合的執行者 |
|---|---|---|---|
| 慢思考 | 日 / 週 | 非結構化文本 | 大型 LLM + RAG |
| 快感知 | 毫秒 / 秒 | 結構化推送 | 券商 API |
| 編排執行 | 事件觸發 | 訊號 + 狀態 | Agent 編排器 |

把這三件事塞進同一層會違反每一個工程原則：耦合過高、責任不清、測試困難、改一處動全身。

## 大腦層：該做與不該做

### 該做

- 把不結構化的長文本綜合成自然語言判斷
- 回答慢問題：「這檔股票的長期論述還站得住嗎？」、「這個總體環境下最該避開的板塊？」、「這份財報裡 management 想藏什麼？」
- 提供「**為什麼這樣判斷**」的可讀理由（人類事後可審查）

### 不該做

- 被即時行情觸發（沒這個反應速度）
- 直接決定下單時機（這是執行層的事）
- 知道任何券商憑證（碰不到、也不該碰）
- 緩存「策略執行狀態」（無狀態最安全）

### 介面契約

大腦層應對外暴露一個「**請給我一份判斷**」的呼叫介面，回傳格式建議：

```json
{
  "judgment": "natural language summary",
  "signals": [
    {"symbol": "HK.00700", "stance": "neutral|bullish|bearish", "confidence": 0.0-1.0}
  ],
  "reasoning": "human-readable explanation",
  "valid_until": "ISO timestamp",
  "sources": ["doc_id_1", "doc_id_2"]
}
```

`valid_until` 是大腦層自己宣告的判斷時效——這個欄位讓執行層知道什麼時候該重新請示。

## 感知層：該做與不該做

### 該做

- 提供當下市場狀態的真相：報價、五檔、Tick、持倉、訂單回報
- 接受訂閱 / 查詢 / 推送
- 提供帳戶資訊（持倉、可用資金、模擬盤狀態）

### 不該做

- 任何決策邏輯
- 任何預測
- 緩存「策略狀態」（state belongs to 執行層）

### 介面契約

感知層對外暴露三類介面：

1. **訂閱式**（推送）：報價更新、訂單成交回報
2. **查詢式**（請求-回應）：當下持倉、歷史 K 線、帳戶資訊
3. **指令式**（單向觸發）：下單、撤單、改單

執行層應同時處理推送與請求，並把所有輸出都標上時間戳。

## 執行層：該做與不該做

### 該做

- 把大腦的慢判斷與感知層的快訊號融合成決策
- 強制執行硬編碼風控（部位上限、單日損失停損、kill switch）
- 發起下單（模擬盤）
- 記錄所有決策的審計日誌
- 推送通知給指揮官（Telegram / Email / Webhook）

### 不該做

- 繞過風控（即使大腦層說「這次例外」）
- 自己生成新策略（那是大腦層的工作）
- 在沒有人類授權下從模擬盤切到實盤
- 修改自己的硬編碼風控參數

### 內部狀態

執行層是整個系統唯一允許持有「策略狀態」的地方。狀態應包括：

- 當前部位（從感知層同步）
- 大腦層最近一次判斷（含 `valid_until`）
- 風控旗標（kill switch 是否啟動、單日損失累計）
- 上次決策的時間與依據

狀態應持久化到本地檔案（建議 SQLite 或 JSONL），系統重啟可復原。

## 失敗模式設計

每一層都應該預期另外兩層會失敗：

- **大腦層失聯**：執行層應有「保守預設」——預設不開新倉、只執行清倉指令
- **感知層失聯**：執行層應立刻進入安全模式，記錄日誌並通知指揮官
- **執行層失聯**：感知層的訊號被丟棄，大腦層的判斷成為孤兒——這是最該避免的失敗，所以執行層應該是最簡單、最可預測的一層

## 部署拓撲

最小化拓撲：三層全部跑在指揮官的本機。

進階拓撲：
- 大腦層的長文本檢索可放雲端（NotebookLM 本來就是雲端服務）
- 感知層的網關（如富途 OpenD）必須跑在指揮官能控制的機器上（避免帳號風控與安全問題）
- 執行層應與感知層同機部署，降低延遲與斷線風險

不建議把執行層放在第三方雲端——理由見 `boundaries.md` 紅線一節。

---

*這是設計參考，不是實作手冊。具體的 SDK、port、認證方式請 agent 自行查詢官方文件。*
